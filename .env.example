# Copy to .env and fill in. Never commit a filled-in copy — this repository
# becomes public, with its full history.

# Base URL of the FTNSS API the panel queries for nearby gyms.
FTNSS_API_BASE_URL=

# Build channel: development | production
BUILD_CHANNEL=development
